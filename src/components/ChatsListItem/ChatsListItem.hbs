<button class="chats-list-item {{#if isActive}} chats-list-item_active {{/if}}" type="button">
	{{> base-avatar 
		alt="Аватар пользователя {{ last_message.user.first_name }} {{ last_message.user.second_name }}" 
		url="{{ avatar }}" 
	}}
	<div class="chats-list-item__content">
		<span class="chats-list-item__title base-text">{{ title }}</span>
		<span class="chats-list-item__text base-text base-text_size_xs">
			<span class="chats-list-item__text base-text base-text_weight_bold">{{ last_message.user.first_name }}:</span> 
			{{ last_message.content }}
		</span>
	</div>
	{{#if unread_count}}
		<span 
			class="chats-list-item__counter base-text base-text_size_xs rect-size"
			aria-label="Непрочитанных соощений: {{ unread_count }}"	
		>
			{{ unread_count }}
		</span>
	{{/if}}
</button>